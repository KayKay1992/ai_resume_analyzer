 integration of uploadFile and form component 

Recent changes summary:

1. Added `react-dropzone` to dependencies for drag-and-drop file upload support.
2. Created `FileUploader` component using `react-dropzone` for PDF uploads, file size formatting, and removal.
3. Added `formatSize` utility in `app/lib/utils.ts` for human-readable file size display.
4. Created `upload.tsx` route with a form for company name, job title, job description, and resume upload, using the `FileUploader` component.
5. Updated `routes.ts` to include the new `/upload` route.
6. The upload form manages its own state, simulates processing, and displays a status message after upload.
7. The `FileUploader` component now manages its own file state, preventing re-upload when removing a file.